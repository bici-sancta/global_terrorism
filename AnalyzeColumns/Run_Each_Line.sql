/* SQL WORKBENCH ONLY SHOWS 50 tabs at a time *
/* Run each statement separately to analyze the 127 columns */

call EnumerateTableValues(0);
call EnumerateTableValues(50);
call EnumerateTableValues(150);

